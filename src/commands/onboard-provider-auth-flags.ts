import {
  CORE_ONBOARD_AUTH_FLAGS,
  type OnboardCoreAuthFlag,
} from "./onboard-core-auth-flags.js";

export type OnboardProviderAuthFlag = OnboardCoreAuthFlag;

export const ONBOARD_PROVIDER_AUTH_FLAGS = CORE_ONBOARD_AUTH_FLAGS;
